Study `specs/projects-ui-integration.md` to learn the requirements.
Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
Study `src/components/panels/task-board-panel.tsx` to understand the existing code — specifically TaskDetailModal, CreateTaskModal, and the board header.
Study `src/app/api/tasks/` to understand the existing task API.
Study `src/app/api/projects/` to understand the existing project API.

Compare specs against code (gap analysis). Create/update @IMPLEMENTATION_PLAN.md
with a prioritized bullet list of items to implement.

IMPORTANT: Plan only. Do NOT implement anything.
Do NOT assume functionality is missing — search the code first.
If specs are missing for a discovered need, create them in specs/.
When plan is ready, add at the bottom: STATUS: PLANNING_COMPLETE
