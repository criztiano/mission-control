Study `specs/*` to learn the feature specifications.
Study `src/components/panels/agent-detail-tabs.tsx` and `src/components/panels/agent-squad-panel-phase3.tsx` to understand the existing agent detail modal and tab system.
Study `src/components/ui/` to understand available UI primitives (Button, PropertyChip, BlockEditor).
Study `src/app/api/agents/` to understand existing agent API patterns.
Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.

Compare specs against existing code (gap analysis). Create/update @IMPLEMENTATION_PLAN.md with a prioritized bullet list of items to implement.

IMPORTANT: Plan only. Do NOT implement anything.
Do NOT assume functionality is missing — search the codebase first.
Update STATUS.md with what you analyzed.
