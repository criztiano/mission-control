// Migrations are now handled by drizzle-kit.
// Run: npx drizzle-kit generate && npx drizzle-kit migrate
// This file is kept as a no-op stub to avoid breaking imports.

export async function runMigrations(): Promise<void> {
  // No-op: schema migrations applied via drizzle-kit
}
